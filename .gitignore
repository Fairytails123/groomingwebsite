# dependencies (NOT currently a junction — a real directory inside OneDrive since 2026-07-16,
# so deps sync between two machines of different CPU architectures. See CLAUDE.md 'Local dev'.)
node_modules/

# build output
dist/
.astro/

# verbatim harvest of the old WordPress site — NEVER commit (large; public repo)
grooming-image-archive/

# hero-animation design handoff — the prototypes + their source artwork (~3.5MB). Same rule as
# the harvest: the bulky SOURCE stays out of the public repo, while the derived assets
# (src/assets/pages/home/hero-*.png and hero-*.webp) and the script that makes them
# (scripts/hero-assets.mjs)
# are committed. Re-run that script only if this folder's artwork changes.
Luxury dog grooming animation/

# viewport-gate screenshots (scripts/shots.mjs output)
shots/

# misc
*.log
.DS_Store
Thumbs.db
